#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../d20/bin/d20" "$@"
  ret=$?
else 
  node  "$basedir/../d20/bin/d20" "$@"
  ret=$?
fi
exit $ret
